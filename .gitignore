node_modules
__pycache__/
*.py[cod]
clickhouse_data
.env
.papi

# Typegen inputs, both regenerated by `npm run typegen`:
#   the ~19MB runtime metadata index (see README), and the type bundle
#   materialised from src/basiliskTypesBundle.ts
typegen/basiliskVersions.jsonl
typegen/basilisk-types.json
logs/

docker-compose.override.yml
test-results/
.playwright-mcp/

/*.png

# planning/spec/report scratch (AGENTS: do not commit task plans, agent reports)
docs/superpowers/
